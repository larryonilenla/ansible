- name: Remove CD-ROM from sources.list
  lineinfile:
    path: /etc/apt/sources.list
    regexp: '^deb cdrom:'
    state: absent
  become: true


- name: Setup Docker on Hosts
  hosts: ubuntuvm3
  become: true
  roles:
    - role: geerlingguy.docker